directive @goTag(
  key: String!
  value: String
) on INPUT_FIELD_DEFINITION | FIELD_DEFINITION

type Tivo {
  name: String!
  address: String!
  tsn: String!
}

type Query {
  tivos: [Tivo!]!
}

schema {
  query: Query
}
