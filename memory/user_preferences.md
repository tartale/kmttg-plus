---
name: user-preferences
description: "tartale's working style — terse, decisive, commits frequently, tests against real data"
metadata: 
  node_type: memory
  type: user
  originSessionId: 99289808-2fe3-4e7f-80ba-a8f8cda2fada
---

- Prefers short direct answers; no need to explain what was done unless asked
- Commits work incrementally with meaningful messages, pushes to feature branches
- Tests against real recorded TiVo content (Abbott Elementary, etc.)
- Makes decisions quickly and moves on (e.g. "yes, pull it out" / "yes")
- Edits files directly alongside Claude rather than waiting for Claude to do everything
- Three-repo structure: `kmttg-plus` (parent), `kmttg-go` (Go backend), `kmttg-webui` (React/TypeScript frontend)
- Submodule Go/webui repos push with `HEAD:refs/heads/<branch>` syntax when in detached HEAD state
- `server/generated.go` is maintained manually (no codegen in this environment); `model/generated.go` and `graphql-types.ts` ARE regenerated by the user
- Memory markdowns should be saved in `/workspace/memory/` (project directory), not in `~/.claude/`

**Why:** personal TiVo DVR management tool; goal is to download and strip ads from recordings
